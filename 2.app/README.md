# App patches

The three files here reflect the author's own site: real file/table names and line numbers from
that specific install. Read them as worked examples of the three typical caller types this project
repoints — web, gameplay, worker — and adapt the paths/identifiers to your own setup.

- `admin_actions.web_repoint.md` — web caller (PHP admin panel).
- `usp_Insert_Action_Log_E_change.md` — gameplay caller (stored procedure).
- `worker.notes.md` — worker/task-scheduler caller.
